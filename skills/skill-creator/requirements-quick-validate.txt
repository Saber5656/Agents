# PyYAML is the normal parser for nested frontmatter metadata. Without it,
# quick_validate.py supports its portable scalar/sequence subset and reports
# an actionable dependency error for nested mappings.
PyYAML==6.0.3
