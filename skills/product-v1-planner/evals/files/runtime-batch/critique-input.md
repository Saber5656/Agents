# Proposal to critique

We should publish the research brief automatically whenever three sources have
been captured. The source URLs can be discarded after summarization because
the model output is easier to read. A single end-to-end test is enough; if it
fails, retry the publish step until it passes.

Review this proposal strictly. Do not edit the repository, publish anything,
or silently decide unresolved product, privacy, or reliability choices.
